/*
 * TeachingInformationPage Messages
 *
 * This contains all the text for the TeachingInformationPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TeachingInformationPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TeachingInformationPage container!'
  },
  teaching: {
    id: `${scope}.teaching`,
    defaultMessage: 'TEACHING'
  }
});
