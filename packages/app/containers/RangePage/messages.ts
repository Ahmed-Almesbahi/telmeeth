/*
 * RangePage Messages
 *
 * This contains all the text for the RangePage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.RangePage";

export default defineMessages({
  range: {
    id: `${scope}.range`,
    defaultMessage: "RANGE"
  },
  done: {
    id: `${scope}.done`,
    defaultMessage: "DONE"
  },
  KG: {
    id: `${scope}.KG`,
    defaultMessage: "KG"
  }
});
