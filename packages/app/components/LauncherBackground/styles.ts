import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  containerBackground: {
    // borderColor: "blue",
    // borderWidth: 1,

    flex: 1,
    backgroundColor: 'transparent',
    width: undefined,
    height: undefined
  }
});
