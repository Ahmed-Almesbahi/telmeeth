import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    // marginTop: 20,
    // marginBottom: 20
  },
  buttonContent: {
    height: 40,
    width: 150
  }
});
